# cosoco-classifier
Classification pipeline using various feature reduction strategies for COSOCO.

Uses a fine-tuned PyTorch CNN (ResNet18) as feature extraction, then assesses the performance of various combinations of dimensionality reduction techniques and classifiers (SVM, XGBoost, etc.)

View Jupyter notebook [here](https://calebh13.github.io/cosoco-classifier/notebook.html).
